dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //jcenter() Warning: this repository is going to shut down soon  mavenCentral() has
        //the same repositories as jcenter so it can be removed
    }
}

rootProject.name = "TempText"
include ':app'
